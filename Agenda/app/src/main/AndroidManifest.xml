<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agenda" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Adapter.ListaVendaAdapter" />
        <activity android:name=".menu_formulario" />
        <activity android:name=".activity.Venda.ListaVenda" />
        <activity android:name=".activity.Venda.FormVenda" />
        <activity android:name=".activity.Produto.list_produto" />
        <activity android:name=".activity.Produto.FormProduto" />
        <activity android:name=".activity.Cliente.ListaCliente" />
        <activity android:name=".activity.Cliente.FormCliente" />
        <activity android:name=".activity.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>